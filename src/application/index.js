// Application layer entry — place to export domain/application-level use-cases
// Example: export functions that orchestrate data + domain logic

export const placeholder = () => {
  throw new Error('No application use-cases implemented yet')
}
